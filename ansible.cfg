[defaults]
inventory = /home/student/ansible-project/inventory
remote_user = root
host_key_checking = True

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = True
